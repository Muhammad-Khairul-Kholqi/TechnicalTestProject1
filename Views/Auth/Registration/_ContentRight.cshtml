<div class="hidden lg:flex lg:w-1/2 relative bg-gradient-to-br from-[#FE5F00]/10 to-[#ff8d4d]/10">
    <div class="flex flex-col justify-center items-center p-12 w-full">
        <div class="bg-white/20 backdrop-blur-lg rounded-2xl p-8 border border-white/30 shadow-xl w-full max-w-2xl">
            <h1 class="text-4xl font-bold mb-6 text-gray-800">
                Start Managing Your
                <span class="text-[#FE5F00]">Products</span>
            </h1>
            
            <div class="grid grid-cols-2 gap-6 mb-8">
                <div class="bg-white/40 p-5 rounded-xl border border-white/30">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="p-2 bg-[#FE5F00]/10 rounded-lg">
                            <i data-lucide="plus" class="w-6 h-6 text-[#FE5F00]"></i>
                        </div>
                        <h3 class="font-semibold">Easy Setup</h3>
                    </div>
                    <p class="text-sm text-gray-600">Quick account creation and onboarding process</p>
                </div>
                
                <div class="bg-white/40 p-5 rounded-xl border border-white/30">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="p-2 bg-[#FE5F00]/10 rounded-lg">
                            <i data-lucide="sliders" class="w-6 h-6 text-[#FE5F00]"></i>
                        </div>
                        <h3 class="font-semibold">Full Control</h3>
                    </div>
                    <p class="text-sm text-gray-600">Manage products and categories effortlessly</p>
                </div>
                
                <div class="bg-white/40 p-5 rounded-xl border border-white/30">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="p-2 bg-[#FE5F00]/10 rounded-lg">
                            <i data-lucide="shield" class="w-6 h-6 text-[#FE5F00]"></i>
                        </div>
                        <h3 class="font-semibold">Secure Storage</h3>
                    </div>
                    <p class="text-sm text-gray-600">Bank-level security for your data</p>
                </div>
                
                <div class="bg-white/40 p-5 rounded-xl border border-white/30">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="p-2 bg-[#FE5F00]/10 rounded-lg">
                            <i data-lucide="clock" class="w-6 h-6 text-[#FE5F00]"></i>
                        </div>
                        <h3 class="font-semibold">24/7 Support</h3>
                    </div>
                    <p class="text-sm text-gray-600">Round-the-clock customer service</p>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const togglePassword = (inputId, toggleId) => {
            const passwordInput = document.getElementById(inputId);
            const togglePassword = document.getElementById(toggleId);
            
            togglePassword.addEventListener('click', function () {
                if (passwordInput.type === 'password') {
                    passwordInput.type = 'text';
                    this.innerHTML = `<i data-lucide="eye-closed" class="w-5 h-5"></i>`;
                } else {
                    passwordInput.type = 'password';
                    this.innerHTML = `<i data-lucide="eye" class="w-5 h-5"></i>`;
                }
                lucide.createIcons();
            });
        }

        togglePassword('passwordInput', 'togglePassword');
        togglePassword('confirmPasswordInput', 'toggleConfirmPassword');
    });
</script>